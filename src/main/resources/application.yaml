spring:
  application:
    name: TODO
  datasource:
    url: jdbc:mysql://localhost:3306/TODO
    username: root
    password: {your-password}
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
server:
  port: 8080